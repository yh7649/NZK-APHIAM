# Claude Code Instructions

This repo's working conventions are maintained in [`AGENTS.md`](AGENTS.md) —
follow those rules before considering any task complete. They apply
regardless of which agent (Codex, Claude, etc.) is doing the work, so keep
a single copy there rather than duplicating it here.
